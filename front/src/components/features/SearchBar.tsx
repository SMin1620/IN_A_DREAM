import React from "react";
import S from "styled-components";
import { LoginInput } from "../../types/LoginTypes";

export interface SearchBar extends LoginInput {
  borderRadius?: string | number;
  color?: string;
}

const SearchBar = () => {
  return <div></div>;
};

export default SearchBar;
